# DSMS Admin Frontend env vars (Create React App)
# Copy to .env (do not commit .env) and adjust per environment.
#
# Base URL precedence:
# 1) REACT_APP_API_BASE (preferred)
# 2) REACT_APP_BACKEND_URL (fallback)
# 3) http://localhost:8080 (default)

REACT_APP_API_BASE=http://localhost:8080
REACT_APP_BACKEND_URL=http://localhost:8080

REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_WS_URL=ws://localhost:8080

REACT_APP_NODE_ENV=development
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/health

# JSON string objects are supported
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false
REACT_APP_ENABLE_SOURCE_MAPS=true

# Highest-precedence API mode switch:
# - true  => enable real network requests via fetch (Authorization header auto-attached if token exists)
# - false => force stub/in-memory mode for all modules (default)
REACT_APP_USE_NETWORK=false

# When true, authentication and user profile are stubbed (useful when backend is unavailable),
# even if REACT_APP_USE_NETWORK=true.
REACT_APP_USE_STUBS=true
